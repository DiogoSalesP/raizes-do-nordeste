spring:
  application:
    name: raizesdonordeste
  datasource:
    url: jdbc:postgresql://localhost:5432/raizesdonordeste
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true